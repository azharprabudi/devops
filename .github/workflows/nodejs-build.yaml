---
name: Example

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    environment: Example
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo The PR was merged

